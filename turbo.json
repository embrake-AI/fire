{
	"$schema": "https://turbo.build/schema.json",
	"ui": "tui",
	"globalDependencies": [".env"],
	"envMode": "loose",
	"tasks": {
		"build": {
			"dependsOn": ["^build"],
			"outputs": [".next/**", ".output/**", "dist/**", ".vercel/output/**"],
			"inputs": ["$TURBO_DEFAULT$", ".env"]
		},
		"type-check": {},
		"lint": {},
		"dev": {
			"cache": false,
			"persistent": true
		},
		"clean": {
			"cache": false
		}
	}
}
